#define mod (lli)(1e9+7)
#include <iostream>
#include <cmath>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
       long long int n;
       int a[100001];
       for(int i=1;i<=n;i++)
          cin>>a[i];
       int j=1,k=1;
       for(int i=1;i<=n;i++)
       {
         for(int i=1;i<=n;i++)
         {
            if(1 <= abs(j-i) <= a[i])
             
            
         }
       }    
    }
    return 0;
}
 
